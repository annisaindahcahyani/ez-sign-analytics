# =============================================================================
# 📦 MODULE: EZSIGN ANALYTICS ENGINE - SYSTEM DEPENDENCIES (BILL OF MATERIALS)
# =============================================================================
# 📌 CONFIGURATION : Environment Requirements & Package Management
# 📅 UPDATE        : 5 Juni 2026
# 🛡️ OBJECTIVE     : Standardisasi Pustaka Lingkungan Python (Production-Ready)
# =============================================================================
# Instruksi Instalasi:
# Eksekusi perintah berikut pada terminal lingkungan virtual (Virtual Environment):
# $ pip install --no-cache-dir -r requirements.txt
# =============================================================================

# --- [1] CORE DATA PROCESSING & ORM LAYER ---
# Mengelola manipulasi data multidimensi dan persistensi ke sistem basis data relasional.
SQLAlchemy==2.0.30       # Object-Relational Mapping (ORM): Fasilitator transaksi logika Python menuju SQLite.
pandas==2.2.2           # Data Wrangling: Pustaka komputasi DataFrame untuk agregasi, normalisasi, dan rekapitulasi data.
numpy==1.26.4            # Linear Algebra Engine: Komputasi matriks berperforma tinggi untuk evaluasi bobot risiko MADM (SAW/WP).

# --- [2] AUTOMATED FILE SYSTEM OBSERVER ---
# Mekanisme pemantauan subsistem direktori I/O secara real-time.
watchdog==4.0.1          # Event Handler: Pendeteksi anomali/injeksi berkas baru pada direktori staging (Continuous Ingestion).

# --- [3] CRYPTOGRAPHIC & FORENSIC EXTRACTION LAYER ---
# Utilitas pembedahan struktur biner Tanda Tangan Elektronik (TTE) dan Sertifikat Digital.
PyMuPDF==1.24.5          # PDF Engine (fitz): Parser struktur lapisan dasar dokumen PDF berkinerja tinggi.
pikepdf==8.15.1          # Crypto-Extractor: Pengekstrak biner (bytes) objek sertifikat dari dalam struktur PDF.
asn1crypto==1.5.1        # Certificate Decoder: Parser standar ASN.1 untuk dekripsi metadata sertifikat (Issuer, Subject, Expiration).

# --- [4] MICROSERVICE API & MIDDLEWARE INFRASTRUCTURE ---
# Infrastruktur komunikasi terdekopel (Decoupled) antara lapisan komputasi (Engine) dan antarmuka (Frontend).
# Note: Flask & Flask-CORS dieliminasi mutlak demi efisiensi kapasitas image container (Anti-Bloat Protection).
python-dotenv==1.0.1     # Environment Security: Pengelola variabel rahasia (Secret Keys/Paths) dari berkas .env.
fastapi==0.111.0         # Asynchronous Web Framework: Kerangka kerja API berkinerja tinggi untuk gerbang Ingestion.
uvicorn==0.30.1          # ASGI Server: Mesin eksekusi standar industri untuk aplikasi FastAPI.
python-multipart==0.0.9  # Payload Handler: Parser form-data untuk memfasilitasi transmisi unggahan berkas biner via API.

# --- [5] FRONTEND ANALYTICS & VISUALIZATION LAYER ---
# Komponen penyusunan antarmuka visualisasi data operasional (OLAP).
streamlit>=1.37.0       # Declarative UI Framework: Kerangka kerja utama konstruksi antarmuka Dashboard EzSign Analytics.
plotly==5.22.0           # Interactive Visualization: Pustaka perender grafik analitik responsif (Pie/Trend/Bar Chart).
requests==2.32.3         # Synchronous HTTP Client: Jembatan komunikasi REST API dari antarmuka Dashboard menuju Engine.

# --- [6] AUTOMATED WEB INTELLIGENCE & SCRAPING LAYER ---
# Utilitas otomasi peramban siluman untuk validasi aset kriptografi eksternal lintas PSrE.
selenium==4.21.0         # Browser Automation: Kerangka penggerak peramban otomatis (Headless Mode) untuk pemindaian siber.
webdriver-manager==4.0.1 # Driver Utility: Manajer biner otomatis guna memastikan kompatibilitas versi browser driver.
beautifulsoup4==4.12.3   # HTML Parser: Pustaka penunjang untuk ekstraksi tag DOM dan pembersihan data hasil rayapan siber.

streamlit-option-menu
streamlit-authenticator
pyyaml
streamlit-plotly-events